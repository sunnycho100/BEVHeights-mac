# Session Summary — March 18, 2026

## What Was Asked

Port BEVHeight 3D object detection inference to run on Mac CPU (no CUDA/GPU). Execute the plan in `plan/3-18-2026.md` end-to-end: patch all CUDA dependencies, install packages, prepare data, and run evaluation on a local DAIR-V2X-I dataset.

---

## Concise Summary

Successfully ported BEVHeight inference from CUDA-only to Mac CPU. All 6 phases completed:
- Replaced CUDA voxel pooling kernel with pure PyTorch `scatter_add_`
- Patched hardcoded `.cuda()` calls and trainer config for CPU
- Fixed checkpoint loading with `map_location='cpu'`
- Installed all dependencies (mmcv 1.7.0, mmdet 2.28.2, mmdet3d 1.0.0rc6, numba, etc.)
- Generated data info pickles and KITTI labels from local DAIR-V2X-I samples
- Ran full inference (16 val batches, ~49s) and KITTI evaluation — produced AP metrics

**Final eval results (Car AP@0.70):** bbox 88.41/70.54/70.57, BEV 55.34/43.58/43.30, 3D 29.26/21.99/21.88 (Easy/Moderate/Hard).

---

## Detailed Summary (AI Reference)

### Environment

- **OS:** macOS ARM (Apple Silicon)
- **Python:** 3.12.12 (miniforge at `/Users/sunghwan_cho/miniforge/bin/python3`)
- **PyTorch:** 2.10.0 (CPU-only), torchvision 0.25.0
- **PyTorch Lightning:** 1.5.10.post0
- **mmcv:** 1.7.0 (lite, no compiled CUDA extensions)
- **mmdet:** 2.28.2, **mmdet3d:** 1.0.0rc6, **mmsegmentation:** 0.30.0
- **numba:** 0.64.0, **numpy:** 1.26.4, **scipy:** 1.17.1
- **setuptools:** 74.1.3 (must stay <75.0 for `pkg_resources` compatibility)

### Phase 1: Replace CUDA Voxel Pooling

**File:** `ops/voxel_pooling/voxel_pooling.py`
- Added `_voxel_pooling_pure_pytorch()` function using `torch.scatter_add_()` as CPU fallback
- Changed `_CUDA_AVAILABLE = True` → `_CUDA_AVAILABLE = voxel_pooling_ext is not None` (critical fix because `__init__.py` sets `voxel_pooling_ext = None` on import failure, so the import always succeeds)
- Conditional dispatch: uses PyTorch fallback when `_CUDA_AVAILABLE is False`

**File:** `ops/voxel_pooling/__init__.py`
- Wrapped CUDA extension import in try/except: `try: from . import voxel_pooling_ext except ImportError: voxel_pooling_ext = None`

### Phase 2: Patch Hardcoded `.cuda()` Calls

**File:** `layers/backbones/lss_fpn.py` (line 473)
- Changed `self.voxel_num.cuda()` → `self.voxel_num` (registered buffer auto-moves with model)

### Phase 3: Fix Trainer Config for CPU

**File:** `exps/dair-v2x/bev_height_lss_r50_864_1536_128x128_102.py`
- Set `accelerator=None`, `gpus=0`

### Phase 4: Checkpoint Loading

**File:** `exps/dair-v2x/bev_height_lss_r50_864_1536_128x128_102.py`
- Added `torch.load(model_pth, map_location='cpu')` with `model.load_state_dict(ckpt.get('state_dict', ckpt))`

### Phase 5: Dependencies & Data

**Dependency issues resolved:**
- `mmcv==1.7.0` installed with `--no-build-isolation` (pip build isolation breaks `pkg_resources`)
- `mmdet3d==1.0.0rc6` installed with `--no-deps --no-build-isolation` (skips bad numba/networkx pins)
- Additional deps: `mmsegmentation==0.30.0`, `lyft-dataset-sdk`, `trimesh`, `plyfile`, `pyquaternion`, `nuscenes-devkit`, `tensorboardX`, `scikit-image`, `pypcd3`

**External package patches (in site-packages, must re-apply if packages reinstalled):**

1. **`mmcv/utils/ext_loader.py`** — `load_ext()` returns stub modules with None attributes when compiled CUDA extensions are unavailable
2. **`mmcv/ops/deform_conv.py`** — Added `torchvision.ops.deform_conv2d` CPU fallback when `ext_module.deform_conv_forward is None`

**Data preparation:**
- Symlinked `data/dair-v2x-i/` → `single-infrastructure-side/`
- Created `data/single-infrastructure-split-data-local.json` (55 train + 16 val sample IDs)
- Generated `dair_12hz_infos_train.pkl` (55 samples) and `dair_12hz_infos_val.pkl` (16 samples) via `scripts/gen_info_dair.py`
- Generated 71 KITTI-format label files in `data/dair-v2x-i-kitti/training/label_2/` via `scripts/data_converter/dair2kitti.py`

**Other files created/modified:**
- `setup.py` — skip `ext_modules` when no CUDA: `ext_modules=[...] if cuda_available else []`
- `scripts/vis_utils.py` — stub with dummy `demo()` function (import target)
- `checkpoints/` — directory with symlink to `.ckpt` file

### Phase 6: Verification (Evaluation Run)

**File:** `evaluators/kitti_utils/rotate_iou.py`
- Rewrote entirely: replaced all `@cuda.jit` decorators with `@numba.njit`, replaced `cuda.local.array()` with `np.zeros()`, replaced CUDA kernel grid/block dispatch with simple nested for-loops
- Entry point `rotate_iou_gpu_eval()` signature preserved for API compatibility

**Run command:**
```bash
cd /Users/sunghwan_cho/Documents/CATS_lab/BEVHeights-mac
python3 exps/dair-v2x/bev_height_lss_r50_864_1536_128x128_102.py -e -b 1 --ckpt_path ./checkpoints
```

**Results:**
```
Car AP@0.70, 0.70, 0.70:
  bbox AP: 88.41, 70.54, 70.57
  bev  AP: 55.34, 43.58, 43.30
  3d   AP: 29.26, 21.99, 21.88

Car AP@0.70, 0.50, 0.50:
  bbox AP: 88.41, 70.54, 70.57
  bev  AP: 89.75, 72.72, 74.94
  3d   AP: 82.97, 68.30, 68.30

Pedestrian AP@0.50: bbox 51.41/52.92/52.92
Cyclist AP@0.50:    bbox 7.50/10.00/10.00

Overall AP: bbox 49.11/44.48/44.49, BEV 19.00/15.08/14.99, 3D 10.30/7.88/7.85
```

Processing: 16 val batches in ~49 seconds total on CPU.

### Known Caveats

- The two external package patches (`mmcv/utils/ext_loader.py` and `mmcv/ops/deform_conv.py`) live in site-packages and will be lost if mmcv is reinstalled
- Training is NOT supported — `get_targets()` in `bev_height_head.py` still has hardcoded `device='cuda'`
- Only the R50 DAIR-V2X config was modified; rope3d and R101 configs are untouched
- CPU inference is ~3s/batch (vs sub-second on GPU) — expected for CPU-only
